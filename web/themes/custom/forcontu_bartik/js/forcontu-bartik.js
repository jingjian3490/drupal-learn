/**
 * @file
 * Forcontu Bartik behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.forcontuBartik = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
