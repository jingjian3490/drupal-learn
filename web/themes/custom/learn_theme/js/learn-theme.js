/**
 * @file
 * learn_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.learnTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
